# ServiceForge AI — Security & Governance Specification

**Version:** 1.0.0  
**Project:** ServiceForge AI  
**Authentication:** Amazon Cognito User Pool  
**Authorization:** Role-Based Access Control (RBAC) + Multi-Tenant Organization Isolation  
**Encryption:** AWS KMS (At Rest), TLS 1.3 (In Transit)  

---

## 1. Authentication & Token Architecture

ServiceForge AI uses **Amazon Cognito User Pools** for identity management. Upon successful authentication, Cognito issues cryptographically signed JWT tokens (ID Token, Access Token, Refresh Token).

### 1.1 Custom JWT Claims
Every access token issued by Cognito contains injected custom claims:
- `custom:role` — Defines the user's RBAC persona (`ADMIN` | `SERVICE_MANAGER` | `DISPATCHER` | `TECHNICIAN` | `CUSTOMER`).
- `custom:org_id` — Defines the user's multi-tenant organization boundary (e.g. `org-8841-alpha`).

### 1.2 JWT Validation at API Gateway
API Gateway enforces token validation via a **Cognito User Pool Authorizer**:
1. Verifies token signature against Cognito JWKS (JSON Web Key Set).
2. Validates token expiration (`exp`) and audience (`aud`).
3. Injects validated claims into Lambda `event.requestContext.authorizer.claims`.

---

## 2. Role-Based Access Control (RBAC) Matrix

ServiceForge AI enforces strict access control across 5 user roles:

| Resource / Action | ADMIN | SERVICE_MANAGER | DISPATCHER | TECHNICIAN | CUSTOMER |
| :--- | :---: | :---: | :---: | :---: | :---: |
| **Manage Users & Org Settings** | ✅ | ❌ | ❌ | ❌ | ❌ |
| **View Customer Accounts** | ✅ | ✅ | ✅ | ❌ | ❌ |
| **Create Service Request** | ✅ | ✅ | ✅ | ❌ | ✅ (Own Org) |
| **View Service Requests** | ✅ | ✅ | ✅ | ❌ | ✅ (Own Org) |
| **Trigger AI Analysis** | ✅ | ✅ | ✅ | ❌ | ❌ |
| **Approve AI & Create Job** | ✅ | ✅ | ❌ | ❌ | ❌ |
| **Assign Technician to Job** | ✅ | ✅ | ✅ | ❌ | ❌ |
| **View Assigned Jobs** | ✅ | ✅ | ✅ | ✅ (Assigned) | ✅ (Own Org) |
| **Log Field Update / Checklist** | ✅ | ✅ | ❌ | ✅ (Assigned) | ❌ |
| **Generate Service Report** | ✅ | ✅ | ❌ | ❌ | ❌ |
| **Download Final PDF Report** | ✅ | ✅ | ✅ | ✅ | ✅ (Own Org) |
| **Generate Attachment Presigned URL**| ✅ | ✅ | ✅ | ✅ | ✅ |

---

## 3. Multi-Tenant Organization Data Isolation

Multi-tenancy is enforced at the application code layer within AWS Lambda microservices:

1. **Extract Claim:** Lambda retrieves `user_org_id = event['requestContext']['authorizer']['claims']['custom:org_id']`.
2. **Key Prefix Injection:** All DynamoDB query keys automatically prepend `ORG#<user_org_id>#`.
3. **Cross-Tenant Prevention:** Any request attempting to access `ORG#<different_org_id>` is denied immediately with HTTP `403 Forbidden`.

---

## 4. AWS IAM Least-Privilege Execution Policies

Lambda execution roles are restricted to the bare minimum permissions required.

### 4.1 Lambda DynamoDB Access Policy
```json
{
  "Version": "2012-10-17",
  "Statement": [
    {
      "Sid": "DynamoDBTableAccess",
      "Effect": "Allow",
      "Action": [
        "dynamodb:GetItem",
        "dynamodb:PutItem",
        "dynamodb:UpdateItem",
        "dynamodb:Query"
      ],
      "Resource": [
        "arn:aws:dynamodb:ap-south-1:123456789012:table/ServiceForge",
        "arn:aws:dynamodb:ap-south-1:123456789012:table/ServiceForge/index/*"
      ]
    }
  ]
}
```

### 4.2 Lambda Amazon Bedrock Access Policy
```json
{
  "Version": "2012-10-17",
  "Statement": [
    {
      "Sid": "BedrockInvokeModel",
      "Effect": "Allow",
      "Action": [
        "bedrock:InvokeModel"
      ],
      "Resource": [
        "arn:aws:bedrock:ap-south-1::foundation-model/anthropic.claude-3-5-sonnet-20241022-v2:0",
        "arn:aws:bedrock:ap-south-1::foundation-model/anthropic.claude-3-haiku-20240307-v1:0"
      ]
    }
  ]
}
```

---

## 5. Amazon S3 Security & Presigned URLs

- **Private Buckets:** `BlockPublicAccess = true` across all S3 buckets.
- **Short-Lived Presigned URLs:**
  - File uploads (`PutObject`) and downloads (`GetObject`) use S3 Presigned URLs generated by Lambda.
  - Expiration time: **900 seconds (15 minutes)**.
- **Path Isolation:** Objects are isolated by organization: `attachments/orgs/<orgId>/<entityType>/<fileId>`.

---

## 6. Secrets Management & Environment Security

- **Zero Hardcoded Credentials:** No AWS access keys (`AKIA...`) or passwords exist in source code or repositories.
- **IAM Roles:** Compute resources (AWS Lambda) authenticate using short-lived temporary STS credentials provided by execution roles.
- **AWS Parameter Store:** Non-sensitive environment variables (`DYNAMODB_TABLE_NAME`, `LOG_LEVEL`) stored in SSM Parameter Store.

---

## 7. AI Security & Prompt Injection Mitigation

1. **Input Sanitization:** Raw customer issue text is stripped of control characters and prompt injection delimiters (e.g. `System:`, `Ignore previous instructions`) before interpolating into Bedrock prompts.
2. **Bedrock Guardrails:** Content filtering blocks harmful or malicious prompts.
3. **Decision Support Framing:** System prompts explicitly instruct the model to produce output strictly adhering to JSON schema without executing arbitrary code or diagnostic assertions.
